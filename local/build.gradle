plugins {
    id 'library-common-conventions'
}

dependencies {
    implementation project(':core')
    testImplementation project(':testkit')

    // In-memory file system (needed for MLeap serialization of models)
    runtimeOnly libs.memoryFileSystem
}
