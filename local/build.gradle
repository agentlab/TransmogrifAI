dependencies {
    implementation project(':core')
    testImplementation project(':testkit')

    // In-memory file system (needed for MLeap serialization of models)
    runtimeOnly "com.github.marschall:memoryfilesystem:$memoryFilesystemVersion"
}
